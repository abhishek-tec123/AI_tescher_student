# Create deployment documentation as a TXT file using pypandoc
import pypandoc

content = """
AI TEACHER – PRODUCTION DEPLOYMENT DOCUMENTATION
================================================

PROJECT OVERVIEW
----------------
Frontend Domain:
https://tecorb.in

Backend Domain:
https://api.tecorb.in

Server:
AWS EC2 (Ubuntu)

Backend Framework:
FastAPI (Uvicorn)

Internal Backend Port:
3018


1) PROJECT DIRECTORY STRUCTURE
------------------------------
Project Path:
/home/ubuntu/AI_Teacher

Virtual Environment:
/home/ubuntu/AI_Teacher/.AITEACHERvenv

Environment File:
/home/ubuntu/AI_Teacher/.env


2) FASTAPI SYSTEMD SERVICE SETUP
--------------------------------
Service File Location:
/etc/systemd/system/AITEACHER.service

Service Configuration:

[Unit]
Description=AI Teacher FastAPI Server
After=network.target redis.service

[Service]
User=ubuntu
Group=ubuntu
WorkingDirectory=/home/ubuntu/AI_Teacher
EnvironmentFile=/home/ubuntu/AI_Teacher/.env
ExecStart=/home/ubuntu/AI_Teacher/.AITEACHERvenv/bin/python -m uvicorn app:app --host 0.0.0.0 --port 3018
Restart=always
RestartSec=5
TimeoutStopSec=30

[Install]
WantedBy=multi-user.target


SYSTEMD COMMANDS
----------------
Reload systemd:
sudo systemctl daemon-reload

Enable service (start on boot):
sudo systemctl enable AITEACHER

Start service:
sudo systemctl start AITEACHER

Restart service:
sudo systemctl restart AITEACHER

Check status:
sudo systemctl status AITEACHER

View live logs:
sudo journalctl -u AITEACHER -f


3) NGINX REVERSE PROXY SETUP
----------------------------
NGINX Config File:
/etc/nginx/sites-available/api

Configuration:

server {
    server_name api.tecorb.in;

    location / {
        proxy_pass http://127.0.0.1:3018;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

Enable configuration:
sudo ln -s /etc/nginx/sites-available/api /etc/nginx/sites-enabled/

Test nginx config:
sudo nginx -t

Restart nginx:
sudo systemctl restart nginx


4) SSL SETUP (LET'S ENCRYPT)
----------------------------
Install certbot:
sudo apt install certbot python3-certbot-nginx -y

Generate SSL certificate:
sudo certbot --nginx -d api.tecorb.in

Certificate Location:
/etc/letsencrypt/live/api.tecorb.in/

Test auto-renew:
sudo certbot renew --dry-run


5) FASTAPI CORS CONFIGURATION
------------------------------
Add inside FastAPI app:

from fastapi.middleware.cors import CORSMiddleware

origins = [
    "https://tecorb.in",
]

app.add_middleware(
    CORSMiddleware,
    allow_origins=origins,
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

Restart service after editing:
sudo systemctl restart AITEACHER


6) TESTING CHECKLIST
--------------------
Test backend internally:
curl http://127.0.0.1:3018/health

Test backend via domain:
https://api.tecorb.in/health

Check service status:
sudo systemctl status AITEACHER

Check nginx:
sudo systemctl status nginx


7) FINAL PRODUCTION ARCHITECTURE
--------------------------------
User
  ↓
Frontend (HTTPS)
  ↓
NGINX (Port 443, SSL Enabled)
  ↓
FastAPI (127.0.0.1:8000 Internal HTTP)

Port 8000 is NOT publicly exposed.
All external traffic goes through HTTPS only.


8) COMMON TROUBLESHOOTING
-------------------------
502 Bad Gateway:
- FastAPI service not running
- Check: sudo systemctl status AITEACHER

CORS Error:
- Check FastAPI CORS configuration
- Restart service

SSL Error:
- Run: sudo certbot renew

DNS Not Working:
- Verify A record for api.tecorb.in points to EC2 public IP


DEPLOYMENT COMPLETE
-------------------
Backend is:
- Running via systemd
- Auto-start on reboot
- Secured with HTTPS
- Reverse proxied via NGINX
- SSL auto-renew enabled
"""